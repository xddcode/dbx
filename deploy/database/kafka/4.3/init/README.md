# Kafka 4.3 smoke data

Kafka runs in a single-node KRaft configuration. The `verify` command creates the `dbx-smoke` topic, publishes `DBX smoke`, and consumes it through the internal broker listener.
